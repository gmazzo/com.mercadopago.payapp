apply plugin: 'com.android.application'
apply from: '../shared.gradle'

android {
    defaultConfig {
        versionCode 1
        versionName '1.0'
    }
}

dependencies {
    implementation project(':data')
}
